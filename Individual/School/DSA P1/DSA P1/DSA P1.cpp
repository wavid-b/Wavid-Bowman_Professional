#include <iostream> 
#include <string>
#include <chrono>
#include "tree.h"
#include "helpers.h"
/* to do
    remove
        removing root has issues when other insertions are done first - i think
    removeInorder
        needs work
    make time complexity of setbalance and check balance better
    finish parser

*/
using namespace std;
using namespace std::chrono;
int main() {
    parseInput();
}